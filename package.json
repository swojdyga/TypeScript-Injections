{
  "name": "typescript-injections",
  "version": "0.0.1-alpha-1",
  "description": "TypeScript Injections",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "build": "(rimraf -r build/* && tsc) || rimraf -r build/*",
    "main": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chai": "^4.2.7",
    "@types/mocha": "^5.2.7",
    "@types/node": "^10.17.9",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "ts-loader": "^6.2.1",
    "ts-loader-decleration": "^0.10.4",
    "ts-node": "^7.0.1",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "typescript-class-types": "^1.1.0"
  }
}
